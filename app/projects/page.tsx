import Link from "next/link";
import { ArrowLeft, ArrowUpRight, Github } from "lucide-react";
import { PageShell } from "@/components/page-shell";
import { projects } from "@/data/site";

export default function Projects(){return <PageShell><section className="container py-24"><Link href="/" className="inline-flex items-center gap-2 text-sm text-[#91a79a] hover:text-white"><ArrowLeft size={16}/> Home</Link><div className="mt-10 max-w-3xl"><p className="text-xs uppercase tracking-[0.2em] text-[#7cae89]">Portfolio</p><h1 className="mt-3 text-5xl font-semibold tracking-[-0.03em] md:text-6xl">Projects with an engineering point of view.</h1><p className="mt-6 text-lg leading-8 text-[#91a79a]">The portfolio grows around evidence: what I built, why I built it, how it works, how I secured it, and what I learned.</p></div><div className="mt-16 grid gap-5 lg:grid-cols-2">{projects.map(p=><article key={p.slug} className="glass rounded-3xl p-7"><div className="flex items-center justify-between"><span className="text-xs uppercase tracking-[0.16em] text-[#7cae89]">{p.type}</span><span className="rounded-full border border-white/10 px-2.5 py-1 text-[10px] text-[#758b7d]">{p.status}</span></div><h2 className="mt-8 text-2xl font-semibold">{p.title}</h2><p className="mt-3 leading-7 text-[#91a79a]">{p.summary}</p><div className="mt-6 flex flex-wrap gap-2">{p.stack.map(s=><span key={s} className="rounded-full border border-white/8 bg-white/[0.03] px-2.5 py-1 text-[11px] text-[#91a79a]">{s}</span>)}</div><div className="mt-7 border-t border-white/5 pt-5"><p className="text-xs uppercase tracking-[0.14em] text-[#60756a]">Why it matters</p><p className="mt-2 text-sm leading-6 text-[#c7d7cb]">{p.outcome}</p></div><div className="mt-6 flex gap-4"><Link href={`/projects/${p.slug}`} className="inline-flex items-center gap-2 text-sm text-[#b5f6c6]">Case study <ArrowUpRight size={15}/></Link><a href={p.github} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 text-sm text-[#91a79a] hover:text-white"><Github size={15}/> GitHub</a></div></article>)}</div></section></PageShell>}
